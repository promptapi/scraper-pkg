function scraper(url, params, timeout=5000){
  console.log('url', url);
  console.log('params', params);
  console.log('timeout', timeout);
  // return 'it works...'
}

module.exports = {
  scraper,
}
